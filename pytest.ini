[pytest]
asyncio_mode = auto
testpaths = src/tests
markers =
    e2e: End-to-end tests requiring running stack
    unit: Fast unit tests
    integration: Integration tests with mocked dependencies
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning
